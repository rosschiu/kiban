# GENERATED by scripts/gen-quickstart-compose.sh from infra/compose.yaml — do not edit by hand;
# run `make quickstart-compose` (checked by `make docs-freshness-check`). Read README.md first.
#
# Kiban zero-clone quickstart: every image is `ghcr.io/rosschiu/kiban-<service>:${KIBAN_VERSION}`
# (plus `postgres:18-alpine`); there is no `build:` and no repo checkout. Compared with the source
# file: `migrate`/`bootstrap`/`keycloak` ship as GHCR images, `gateway-devcert` and the `test`
# profile fixtures are dropped, the gateway runs its dev-only plain-HTTP listener on host port
# 3000 (no TLS), and the postgres init SQL is inlined as a compose `config`.
#
# Default port 3000 is deliberate: with no KIBAN_DOMAIN, bootstrap registers only fixed
# dev-loopback redirect URIs (http://localhost:3000, :5173) on the realm, so a browser login only
# completes from that origin. Use the SAME host:port every time you sign in — KEYCLOAK_ISSUER_URL
# is a string compared against the token's `iss` claim.
services:
  postgres:
    image: postgres:18-alpine@sha256:d3e1620b530c944afa6e887d22eb899824da68e19c52024bf98f5220c88a65b2
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set a strong value in .env}
      POSTGRES_DB: ${POSTGRES_DB:-kiban}
      KC_DB_PASSWORD: ${KC_DB_PASSWORD:?set a strong value in .env}
      KIBAN_DB_PASSWORD: ${KIBAN_DB_PASSWORD:?set a strong value in .env}
    volumes:
    - kiban-quickstart-pgdata:/var/lib/postgresql
    ports:
    - 127.0.0.1:${POSTGRES_HOST_PORT:-25432}:5432
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-postgres}
      interval: 5s
      timeout: 5s
      retries: 10
    configs:
    - source: 01_keycloak_db
      target: /docker-entrypoint-initdb.d/01-keycloak-db.sh
    - source: 02_kiban_owner
      target: /docker-entrypoint-initdb.d/02-kiban-owner.sh
  keycloak:
    image: ghcr.io/rosschiu/kiban-keycloak:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KC_HEALTH_ENABLED: 'true'
      KC_HTTP_ENABLED: 'true'
      KC_HOSTNAME_STRICT: 'false'
      KC_PROXY_HEADERS: xforwarded
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KC_DB_PASSWORD:?set a strong value in .env}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KC_BOOTSTRAP_ADMIN_USERNAME:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KC_BOOTSTRAP_ADMIN_PASSWORD:?set a strong value in .env}
      KEYCLOAK_IDENTITY_CLIENT_SECRET: ${KEYCLOAK_IDENTITY_CLIENT_SECRET:?set a strong value in .env}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
    - 127.0.0.1:${KEYCLOAK_HOST_PORT:-25081}:8080
    healthcheck:
      test:
      - CMD-SHELL
      - "exec 3<>/dev/tcp/127.0.0.1/9000 && printf 'GET /health/ready HTTP/1.1\r\nHost: localhost\r\nConnection: close\r\n\r\n' >&3 && grep -q '\"status\": \"UP\"' <&3"
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s
  bootstrap:
    image: ghcr.io/rosschiu/kiban-bootstrap:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_DB_HOST: postgres
      KIBAN_DB_PORT: '5432'
      POSTGRES_DB: ${POSTGRES_DB:-kiban}
      KIBAN_DB_PASSWORD: ${KIBAN_DB_PASSWORD:?set a strong value in .env}
      KIBAN_REGISTRY_DB_PASSWORD: ${KIBAN_REGISTRY_DB_PASSWORD:?set a strong value in .env}
      KIBAN_IDENTITY_DB_PASSWORD: ${KIBAN_IDENTITY_DB_PASSWORD:?set a strong value in .env}
      KIBAN_ORG_DB_PASSWORD: ${KIBAN_ORG_DB_PASSWORD:?set a strong value in .env}
      KIBAN_AUTHZ_DB_PASSWORD: ${KIBAN_AUTHZ_DB_PASSWORD:?set a strong value in .env}
      KEYCLOAK_MANAGEMENT_URL: http://keycloak:9000
      KEYCLOAK_REALM_DISCOVERY_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/.well-known/openid-configuration
      KEYCLOAK_ADMIN_BASE_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-kiban}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KC_BOOTSTRAP_ADMIN_USERNAME:-admin}
      KIBAN_SECRETS_DIR: /run/kiban-secrets
      KIBAN_SUPERADMIN_USERNAME: ${KIBAN_SUPERADMIN_USERNAME:-superadmin}
      KIBAN_SUPERADMIN_EMAIL: ${KIBAN_SUPERADMIN_EMAIL:-superadmin@kiban.local}
      KIBAN_SUPERADMIN_PASSWORD_FILE: /run/kiban-secrets-in/superadmin-password
      KIBAN_REGISTRY_DB_HOST: postgres
      KIBAN_REGISTRY_DB_PORT: '5432'
      KIBAN_REGISTRY_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_AUTHZ_DB_HOST: postgres
      KIBAN_AUTHZ_DB_PORT: '5432'
      KIBAN_AUTHZ_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_ORG_DB_HOST: postgres
      KIBAN_ORG_DB_PORT: '5432'
      KIBAN_ORG_DB_NAME: ${POSTGRES_DB:-kiban}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KC_BOOTSTRAP_ADMIN_PASSWORD:?set a strong value in .env}
    volumes:
    - ./secrets-in:/run/kiban-secrets-in:ro
    - kiban-quickstart-bootstrap-secrets:/run/kiban-secrets
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: 'no'
  migrate:
    image: ghcr.io/rosschiu/kiban-migrate:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-kiban}
      KIBAN_DB_PASSWORD: ${KIBAN_DB_PASSWORD:?set a strong value in .env}
      KIBAN_REGISTRY_DB_PASSWORD: ${KIBAN_REGISTRY_DB_PASSWORD:?set a strong value in .env}
      KIBAN_IDENTITY_DB_PASSWORD: ${KIBAN_IDENTITY_DB_PASSWORD:?set a strong value in .env}
      KIBAN_ORG_DB_PASSWORD: ${KIBAN_ORG_DB_PASSWORD:?set a strong value in .env}
      KIBAN_AUTHZ_DB_PASSWORD: ${KIBAN_AUTHZ_DB_PASSWORD:?set a strong value in .env}
      KIBAN_NOTIFICATION_DB_PASSWORD: ${KIBAN_NOTIFICATION_DB_PASSWORD:?set a strong value in .env}
      KIBAN_TIMESHEET_DB_PASSWORD: ${KIBAN_TIMESHEET_DB_PASSWORD:?set a strong value in .env}
      KIBAN_DOCS_DB_PASSWORD: ${KIBAN_DOCS_DB_PASSWORD:?set a strong value in .env}
      KIBAN_HELPDESK_DB_PASSWORD: ${KIBAN_HELPDESK_DB_PASSWORD:?set a strong value in .env}
    depends_on:
      postgres:
        condition: service_healthy
    restart: 'no'
  registry:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-registry:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_REGISTRY_DB_HOST: postgres
      KIBAN_REGISTRY_DB_PORT: '5432'
      KIBAN_REGISTRY_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_REGISTRY_DB_PASSWORD: ${KIBAN_REGISTRY_DB_PASSWORD:?set a strong value in .env}
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
      KIBAN_INSTALLED_MODULES: notification,timesheet,docs,helpdesk
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8110/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  identity:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-identity:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_IDENTITY_DB_HOST: postgres
      KIBAN_IDENTITY_DB_PORT: '5432'
      KIBAN_IDENTITY_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_IDENTITY_DB_PASSWORD: ${KIBAN_IDENTITY_DB_PASSWORD:?set a strong value in .env}
      KEYCLOAK_BASE_URL: http://keycloak:8080
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/protocol/openid-connect/certs
      KEYCLOAK_ISSUER_URL: http://localhost:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}/realms/${KEYCLOAK_REALM:-kiban}
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-kiban}
      KEYCLOAK_IDENTITY_CLIENT_SECRET_FILE: /run/kiban-secrets/keycloak-identity-client-secret
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
    volumes:
    - kiban-quickstart-bootstrap-secrets:/run/kiban-secrets:ro
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8120/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  org:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-org:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_ORG_DB_HOST: postgres
      KIBAN_ORG_DB_PORT: '5432'
      KIBAN_ORG_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_ORG_DB_PASSWORD: ${KIBAN_ORG_DB_PASSWORD:?set a strong value in .env}
      KIBAN_IDENTITY_BASE_URL: http://identity:8120
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      identity:
        condition: service_healthy
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8130/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  authz:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-authz:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_AUTHZ_DB_HOST: postgres
      KIBAN_AUTHZ_DB_PORT: '5432'
      KIBAN_AUTHZ_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_AUTHZ_DB_PASSWORD: ${KIBAN_AUTHZ_DB_PASSWORD:?set a strong value in .env}
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/protocol/openid-connect/certs
      KEYCLOAK_ISSUER_URL: http://localhost:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}/realms/${KEYCLOAK_REALM:-kiban}
      KIBAN_IDENTITY_BASE_URL: http://identity:8120
      KIBAN_REGISTRY_BASE_URL: http://registry:8110
      KIBAN_ORG_BASE_URL: http://org:8130
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
      identity:
        condition: service_healthy
      registry:
        condition: service_healthy
      org:
        condition: service_healthy
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8140/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  notification:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-notification:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_NOTIFICATION_DB_HOST: postgres
      KIBAN_NOTIFICATION_DB_PORT: '5432'
      KIBAN_NOTIFICATION_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_NOTIFICATION_DB_PASSWORD: ${KIBAN_NOTIFICATION_DB_PASSWORD:?set a strong value in .env}
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/protocol/openid-connect/certs
      KEYCLOAK_ISSUER_URL: http://localhost:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}/realms/${KEYCLOAK_REALM:-kiban}
      KIBAN_ORG_BASE_URL: http://org:8130
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
      KIBAN_NOTIFICATION_SMTP_HOST: localhost
      KIBAN_NOTIFICATION_SMTP_PORT: '1025'
      KIBAN_NOTIFICATION_WEBHOOK_HMAC_SECRET: ${KIBAN_NOTIFICATION_WEBHOOK_HMAC_SECRET:?set a strong value in .env}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
      org:
        condition: service_healthy
      registry:
        condition: service_healthy
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8150/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  timesheet:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-timesheet:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_TIMESHEET_DB_HOST: postgres
      KIBAN_TIMESHEET_DB_PORT: '5432'
      KIBAN_TIMESHEET_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_TIMESHEET_DB_PASSWORD: ${KIBAN_TIMESHEET_DB_PASSWORD:?set a strong value in .env}
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/protocol/openid-connect/certs
      KEYCLOAK_ISSUER_URL: http://localhost:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}/realms/${KEYCLOAK_REALM:-kiban}
      KIBAN_ORG_BASE_URL: http://org:8130
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
      org:
        condition: service_healthy
      registry:
        condition: service_healthy
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8160/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  docs:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-docs:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_DOCS_DB_HOST: postgres
      KIBAN_DOCS_DB_PORT: '5432'
      KIBAN_DOCS_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_DOCS_DB_PASSWORD: ${KIBAN_DOCS_DB_PASSWORD:?set a strong value in .env}
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/protocol/openid-connect/certs
      KEYCLOAK_ISSUER_URL: http://localhost:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}/realms/${KEYCLOAK_REALM:-kiban}
      KIBAN_ORG_BASE_URL: http://org:8130
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
      KIBAN_NOTIFICATION_BASE_URL: http://notification:8150
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
      org:
        condition: service_healthy
      registry:
        condition: service_healthy
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8170/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  helpdesk:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-helpdesk:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KIBAN_HELPDESK_DB_HOST: postgres
      KIBAN_HELPDESK_DB_PORT: '5432'
      KIBAN_HELPDESK_DB_NAME: ${POSTGRES_DB:-kiban}
      KIBAN_HELPDESK_DB_PASSWORD: ${KIBAN_HELPDESK_DB_PASSWORD:?set a strong value in .env}
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/protocol/openid-connect/certs
      KEYCLOAK_ISSUER_URL: http://localhost:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}/realms/${KEYCLOAK_REALM:-kiban}
      KIBAN_ORG_BASE_URL: http://org:8130
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
      KIBAN_NOTIFICATION_BASE_URL: http://notification:8150
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      bootstrap:
        condition: service_completed_successfully
      org:
        condition: service_healthy
      registry:
        condition: service_healthy
    healthcheck:
      test:
      - CMD
      - curl
      - -sf
      - http://127.0.0.1:8180/ready
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
  gateway:
    read_only: true
    cap_drop:
    - ALL
    security_opt:
    - no-new-privileges:true
    tmpfs:
    - /tmp
    image: ghcr.io/rosschiu/kiban-gateway:${KIBAN_VERSION:?set KIBAN_VERSION in .env}
    environment:
      KIBAN_LISTEN_ADDR: 0.0.0.0
      KEYCLOAK_BASE_URL: http://keycloak:8080
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/${KEYCLOAK_REALM:-kiban}/protocol/openid-connect/certs
      KEYCLOAK_ISSUER_URL: http://localhost:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}/realms/${KEYCLOAK_REALM:-kiban}
      KIBAN_REGISTRY_BASE_URL: http://registry:8110
      KIBAN_AUTHZ_BASE_URL: http://authz:8140
      KIBAN_ORG_BASE_URL: http://org:8130
      KIBAN_IDENTITY_BASE_URL: http://identity:8120
      KIBAN_MODULE_HOST_NOTIFICATION: notification
      KIBAN_MODULE_HOST_TIMESHEET: timesheet
      KIBAN_MODULE_HOST_DOCS: docs
      KIBAN_MODULE_HOST_HELPDESK: helpdesk
      KIBAN_TLS_CERT_FILE: ''
      KIBAN_TLS_KEY_FILE: ''
      KIBAN_STATIC_DIR: /var/www/kiban-shell
      KIBAN_INSECURE_HTTP: 'true'
    ports:
    - 127.0.0.1:${KIBAN_GATEWAY_HTTP_HOST_PORT:-3000}:8090
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      registry:
        condition: service_healthy
      authz:
        condition: service_healthy
      org:
        condition: service_healthy
      notification:
        condition: service_healthy
      timesheet:
        condition: service_healthy
      docs:
        condition: service_healthy
      helpdesk:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8090/)" = 200
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s
volumes:
  kiban-quickstart-bootstrap-secrets: {}
  kiban-quickstart-pgdata: {}
configs:
  01_keycloak_db:
    content: |
      #!/bin/bash
      # SPDX-License-Identifier: Apache-2.0

      # Dedicated Keycloak role + database, separate from the application database. Runs once at
      # initdb (docker-entrypoint-initdb.d) as POSTGRES_USER, the cluster superuser — the ONLY place
      # it is used: Keycloak itself connects as `keycloak` (KC_DB_USERNAME/KC_DB_PASSWORD in
      # infra/compose.yaml), which owns nothing but its own database and cannot connect to the app DB.
      # No `set -e`: a ConfigMap mount (deploy/k8s) is sourced into the image entrypoint, not
      # executed; ON_ERROR_STOP + the entrypoint's own -e fail it either way.
      : "${KC_DB_PASSWORD:?KC_DB_PASSWORD not set}"

      psql -v ON_ERROR_STOP=1 -v pw="$KC_DB_PASSWORD" --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<'SQL'
      CREATE ROLE keycloak LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOINHERIT PASSWORD :'pw';
      CREATE DATABASE keycloak OWNER keycloak;
      REVOKE CONNECT ON DATABASE keycloak FROM PUBLIC;
      SQL
  02_kiban_owner:
    content: |
      #!/bin/bash
      # SPDX-License-Identifier: Apache-2.0

      # `kiban`: the non-superuser owner of the application database (POSTGRES_DB, created by the
      # image for the superuser and handed over here). Every migration (`AUTHORIZATION kiban` in
      # migrations/*/, tern.conf's `user = kiban`), scripts/set-role-password.sh and cmd/bootstrap
      # connect as it. CREATEROLE covers migrations/registry/*_role.sql's CREATE ROLE (PG16+: the
      # creator gets ADMIN OPTION on those roles, which is what ALTER ROLE ... PASSWORD needs); owning
      # the database covers CREATE SCHEMA, GRANT CONNECT and the trusted extensions (pgcrypto,
      # btree_gist). No CREATEDB, no BYPASSRLS, no superuser. Only `kiban` and the roles migrations
      # GRANT CONNECT to may connect (`keycloak` cannot). A cluster created before this script
      # existed has `kiban` as its bootstrap superuser, which cannot be demoted — recreate it
      # (see the Operating page of the documentation).
      : "${KIBAN_DB_PASSWORD:?KIBAN_DB_PASSWORD not set}"
      # The database name is fixed: shipped, checksum-frozen migrations (migrations/registry/
      # 0001/0005/0008/0010/0012) `GRANT CONNECT ON DATABASE kiban` by literal and run first on a fresh
      # cluster, so any other POSTGRES_DB fails at `migrate`. Fail here instead, at first start.
      [ "$POSTGRES_DB" = kiban ] || { echo "POSTGRES_DB must be 'kiban' (got '$POSTGRES_DB'): the shipped migrations name the database by literal — see the Operating page of the documentation" >&2; exit 1; }

      psql -v ON_ERROR_STOP=1 -v pw="$KIBAN_DB_PASSWORD" -v db="$POSTGRES_DB" --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<'SQL'
      CREATE ROLE kiban LOGIN NOSUPERUSER NOCREATEDB CREATEROLE NOBYPASSRLS PASSWORD :'pw';
      ALTER DATABASE :"db" OWNER TO kiban;
      REVOKE CONNECT ON DATABASE :"db" FROM PUBLIC;
      GRANT CONNECT ON DATABASE :"db" TO kiban;
      SQL
